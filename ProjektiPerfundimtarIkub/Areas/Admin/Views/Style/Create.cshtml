@model BuyArt.DomainModels.Style
@{
  ViewBag.Title = "Create";
}

<br />
<h2>Create a New Style</h2>
<br />
<div class="container">
  @using (Html.BeginForm("Create", "Style", FormMethod.Post))
  {
    <div class="form-row">
      <div class="col-md-6">
        @Html.LabelFor(m => m.StyleName)
        @Html.TextBoxFor(m => m.StyleName, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.StyleName, null, new { @class = "text-danger" })
        <br />
        <button type="submit" class="btn btn-success">Create</button>
        @Html.ActionLink("Cancel", "Index", "Style", new { @class = "btn btn-light" })
      </div>
      <div class="form-group col-md-6">
        @Html.ValidationSummary(null, new { @class = "text-danger" })
      </div>
    </div>

  }
</div>


